{
  "name": "zero-flow-ui",
  "version": "1.0.0",
  "private": public,
  "description": "Golang project build automation via Node.js",
  "scripts": {
    "build:go": "go mod tidy && go build -v -o ./my-go-app .",
    "build:android": "gomobile init && go mod tidy && GO111MODULE=on gomobile build -androidapi 21 -target=android -o zero-flow.apk ./example"
  }
}
